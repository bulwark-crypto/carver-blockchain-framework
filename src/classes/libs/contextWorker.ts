/*
const { fork } = require('child_process');

const createContextWorker = (type: string) => {
    const contextProcess = fork('./src/context.js', [type]);
    return contextProcess;
}
export {
    createContextWorker
}*/